import classNames from "classnames";
import React<% if (component.hasState) { %>, { useState }<% } %> from "react";

<% if (component.hasStyles) { %>
import "./<%= component.name %>.scss";
<% } %>

interface <%= component.name %>Props {
    className?: string;
}

<% if (component.hasState) { %>
interface <%= component.name %>State {
    exampleState?: string;
}
<% } %>

function <%= component.name %>(props: <%= component.name %>Props) {
    const { className } = props;
    const exampleClasses = classNames(className, "example-class");
    <% if (component.hasState) { %>
    const [exampleState] = useState<<%= component.name %>State>(0);
    <% } %>

    return (
        <div className={exampleClasses}>
            Function component<% if (component.hasState) { %> with {exampleState} state<% } %>
        </div>
    );
}

export default <%= component.name %>;
