import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Logo from "./Logo";

<Meta
    title="component-library/atoms/Logo"
    component={Logo}
/>

export const Template = (args) => <Logo {...args } />

# Logo Atom component

Logo Atom component.

<Canvas>
    <Story
        name="Logo"
        args={{
            speed: "10"
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={Logo} />
