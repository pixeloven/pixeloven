import { storiesOf } from "@storybook/react";
import React from "react";

import { {{componentName}} } from "@shared/components/{{plural atomicType}}";

import Readme from "./README.md";

storiesOf("Components/{{capitalize (plural atomicType)}}/{{componentName}}", module)
    .add("default", () => {
        return <{{componentName}} className={"story-class"} />;
    }, {
        notes: { markdown: Readme },
    });
