import * as React from "react";

{{#if includeStrings}}import {tStrings} from "./{{componentName}}.strings";{{/if}}
{{#if includeStyles}}import "./{{componentName}}.scss";{{/if}}

interface {{componentName}}Props {
    exampleProp?: string;
}

{{#if includeState}}
interface State {
    exampleState?: string;
}

class {{componentName}} extends React.Component<{{componentName}}Props, State> {
{{else}}
class {{componentName}} extends React.Component<{{componentName}}Props> {
{{/if}}
    public render() {
        const { exampleProp } = this.props;

        {{#if includeStrings}}
        return exampleProp ? <div>{exampleProp} with translated {tStrings.exampleString}</div> : false;
        {{else}}
        return exampleProp ? <div>{exampleProp}</div> : false;
        {{/if}}
    }
}

export default {{componentName}};
