import { configure, shallow } from "enzyme";
import ReactSixteenAdapter from "enzyme-adapter-react-16";
import "jest";
import * as React from "react";

import { {{componentName}} } from "@shared/components/{{plural atomicType}}";

configure({
    adapter: new ReactSixteenAdapter(),
});

describe("Shared/Components/{{capitalize (plural atomicTypes)}}/{{componentName}}", () => {
    it("should not render anything", () => {
        const wrapper = shallow(<{{componentName}} />);

        expect(wrapper.type()).toBeNull();
    });

    it("should render \"div\" with property \"example\"", () => {
        const wrapper = shallow(<{{componentName}} exampleProp={"example"} />)

        expect(wrapper.find("div").text()).toEqual("example");
    });
});
