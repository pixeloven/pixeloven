import React from "react";

import { UniversalRouteComponentProps } from "@pixeloven-react/routing";

function NoMatch(props: UniversalRouteComponentProps) {
    return <p>No Match</p>;
}

export default NoMatch;
