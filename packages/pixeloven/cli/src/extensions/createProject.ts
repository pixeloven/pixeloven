import { PixelOvenToolbox } from "../types";

export default (toolbox: PixelOvenToolbox) => {
    const createProject = async (options: string[]) => {
        //
    };
    toolbox.createProject = createProject;
};
