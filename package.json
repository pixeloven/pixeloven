{
  "name": "pixeloven",
  "author": "Brian Gebel <brian@pixeloven.com>",
  "description": "Framework for build Client and Server JavaScript applications",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pixeloven/pixeloven.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=8.0.0",
    "yarn": ">=1.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "compile": "lerna run compile",
    "precompile": "lerna run compile:clean",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "test:watch": "lerna run test:watch",
    "pretty": "lerna run pretty",
    "prebuild": "lerna run compile:clean",
    "pretest": "lerna run test:clean",
    "build:docs": "typedoc --options typedoc.json",
    "deploy:docs": "ts-node --files tools/gh-pages-publish",
    "report-coverage": "cat ./coverage/lcov.info | coveralls",
    "commit": "git-cz",
    "semantic-release": "semantic-release",
    "semantic-release-prepare": "ts-node tools/semantic-release-prepare",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "{tools}/**/*.ts": [
      "yarn pixeloven-cli lint",
      "yarn pixeloven-cli pretty",
      "git add"
    ],
    "packages/**/*.ts": [
      "lerna run pretty",
      "lerna run lint",
      "git add"
    ]
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-conventional-changelog"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^7.1.2",
    "@commitlint/config-conventional": "^7.1.2",
    "@pixeloven/cli": "^0.1.3",
    "@types/jest": "^23.3.2",
    "@types/node": "^10.11.0",
    "colors": "^1.3.2",
    "commitizen": "^3.0.0",
    "coveralls": "^3.0.2",
    "cross-env": "^5.2.0",
    "cz-conventional-changelog": "^2.1.0",
    "husky": "1.1.3",
    "lerna": "3.4.3",
    "lint-staged": "^7.3.0",
    "lodash.camelcase": "^4.3.0",
    "prompt": "^1.0.0",
    "replace-in-file": "^3.4.2",
    "semantic-release": "^15.9.16",
    "ts-node": "7.0.1",
    "typedoc": "0.13.0",
    "typedoc-plugin-monorepo": "0.1.0"
  }
}
